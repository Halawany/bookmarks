version: '3.9'

services:
  bookmarks:
    build: .
    restart: always
    command: python /bookmarks/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/bookmarks
    ports:
      - 8000:8000
    